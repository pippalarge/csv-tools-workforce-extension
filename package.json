{
  "name": "table-tools-workforce-extension",
  "version": "0.2.0",
  "description": "Amplience Workforce extension: Table Tools — shape tabular data as rows (filter, sort, join, add/drop columns, validate, CSV/JSON adapters)",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "validate": "node validate.mjs",
    "check": "node --test && node validate.mjs"
  }
}
